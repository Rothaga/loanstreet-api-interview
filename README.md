# LoanStreet API Intervew
make sure to update the environment variabales in ```.env``` correctly
also don't forget to run ```npm install```
```npm start``` to start the server
```npm test``` to run the test program